import { AnchormeDirective } from './anchorme.directive';

describe('AnchormeDirective', () => {
  it('should create an instance', () => {
    const directive = new AnchormeDirective();
    expect(directive).toBeTruthy();
  });
});
