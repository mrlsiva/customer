import { ConversionDirective } from './conversion.directive';

describe('ConversionDirective', () => {
  it('should create an instance', () => {
    const directive = new ConversionDirective();
    expect(directive).toBeTruthy();
  });
});
